# ws-audio-example #

An example showing how to send, receive and play audio data via websockets.

Clone, `node server.js`, point browser to http://localhost:8080.

Then fork and make it do something useful.
